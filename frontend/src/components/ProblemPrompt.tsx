export function ProblemPrompt() {
  return (
    <section className="problem-prompt">
      <h2 className="problem-title">Two Sum</h2>
      <p className="problem-body">
        Given an array of integers <code>nums</code> and an integer{" "}
        <code>target</code>, return the indices of the two numbers that add up
        to <code>target</code>. Assume each input has exactly one solution and
        you may not use the same element twice.
      </p>
    </section>
  );
}
